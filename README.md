# Semantic-Library-Benchmarking
Academic library system: fine-tuned SBERT for semantic search (&lt;1 min), PostgreSQL &amp; MongoDB index benchmarking on 70M records with &lt;1.5s latency for standard query fields.
